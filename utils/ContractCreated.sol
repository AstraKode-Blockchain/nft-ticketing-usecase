// SPDX-License-Identifier: MIT
pragma solidity ^0.8.0;

library ContractCreated {
    event _contractCreated(
        address newAddress,
        string metadata,
        string name,
        string image,
        address owner,
        bool refunded,
        uint256 maxInfected,
        string date,
        uint256 itemID
    );
}
